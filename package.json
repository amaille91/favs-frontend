{
  "name": "favs-frontend",
  "version": "1.0.0",
  "description": "favs-frontend",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "spago bundle --bundle-type app --platform browser --outfile static/index.js",
    "start-dev": "node dev/proxy-server.js",
    "start-dev-proxy-prod": "node dev/proxy-server-prod.js",
    "test": "spago test",
    "pw:open-session": "node dev/playwright-open-session.mjs",
    "e2e": "npm run build && playwright test",
    "e2e:headed": "npm run build && playwright test --headed",
    "e2e:ui": "npm run build && playwright test --ui",
    "e2e:install": "playwright install chromium"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.27.3",
    "express": "^4.18.1",
    "http-proxy-middleware": "^2.0.6",
    "playwright": "^1.58.2",
    "spago": "^1.0.3"
  }
}
